// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource postgresql {
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  provider          = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id       String  @id @default(uuid()) @postgresql.Uuid
  name     String  @unique @default("")
  password String
  role     Role?   @relation("User_role", fields: [roleId], references: [id])
  roleId   String? @map("role") @postgresql.Uuid

  @@index([roleId])
}

model Role {
  id                String  @id @default(uuid()) @postgresql.Uuid
  name              String  @default("")
  canCreateItems    Boolean @default(false)
  canManageAllItems Boolean @default(false)
  canSeeOtherUsers  Boolean @default(false)
  canEditOtherUsers Boolean @default(false)
  canManageUsers    Boolean @default(false)
  canManageRoles    Boolean @default(false)
  canUseAdminUI     Boolean @default(false)
  canReadChapters   Boolean @default(false)
  canReadImages     Boolean @default(false)
  users             User[]  @relation("User_role")
}

model Product {
  id                     String  @id @default(uuid()) @postgresql.Uuid
  productTitle           String  @unique @default("")
  description            String  @default("")
  productImage_filesize  Int?
  productImage_extension String?
  productImage_width     Int?
  productImage_height    Int?
  productImage_id        String?
  price                  Int
  priceUnit              String  @default("kr")
  discountPrice          Int?
  status                 String  @default("draft")
}

model Project {
  id                     String   @id @default(uuid()) @postgresql.Uuid
  projectTitle           String   @default("")
  shortDescription       String   @default("")
  fullDescription        String   @default("")
  projectImage_filesize  Int?
  projectImage_extension String?
  projectImage_width     Int?
  projectImage_height    Int?
  projectImage_id        String?
  date                   DateTime @postgresql.Date
  location               String   @default("")
  icon                   Json?
}

model Review {
  id         String   @id @default(uuid()) @postgresql.Uuid
  reviewBy   String   @default("")
  reviewText String   @default("")
  date       DateTime @postgresql.Date
  location   String   @default("")
}

model SiteConfig {
  id                   Int     @id
  siteTitle            String  @default("")
  heroPreamble         Json    @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  heroImage1_filesize  Int?
  heroImage1_extension String?
  heroImage1_width     Int?
  heroImage1_height    Int?
  heroImage1_id        String?
  heroImage2_filesize  Int?
  heroImage2_extension String?
  heroImage2_width     Int?
  heroImage2_height    Int?
  heroImage2_id        String?
  heroImage3_filesize  Int?
  heroImage3_extension String?
  heroImage3_width     Int?
  heroImage3_height    Int?
  heroImage3_id        String?
  heroImage4_filesize  Int?
  heroImage4_extension String?
  heroImage4_width     Int?
  heroImage4_height    Int?
  heroImage4_id        String?
  projectsPreamble     Json    @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  productsPreamble     Json    @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  reviewsPreamble      Json    @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  ourServicesPreamble  Json    @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  ourLocationPreamble  Json    @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
}

model Order {
  id            String    @id @default(uuid()) @postgresql.Uuid
  customerName  String    @default("")
  customerEmail String    @default("")
  orderDetails  Json?
  amount        Int
  paymentId     String    @unique @default("")
  cardName      String    @default("")
  createdAt     DateTime? @default(now())
  status        String    @default("notpayed")
}
