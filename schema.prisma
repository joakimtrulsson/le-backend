// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource postgresql {
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  provider          = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id       String  @id @default(uuid()) @postgresql.Uuid
  name     String  @unique @default("")
  password String
  role     Role?   @relation("User_role", fields: [roleId], references: [id])
  roleId   String? @map("role") @postgresql.Uuid

  @@index([roleId])
}

model Role {
  id                String  @id @default(uuid()) @postgresql.Uuid
  name              String  @default("")
  canCreateItems    Boolean @default(false)
  canManageAllItems Boolean @default(false)
  canSeeOtherUsers  Boolean @default(false)
  canEditOtherUsers Boolean @default(false)
  canManageUsers    Boolean @default(false)
  canManageRoles    Boolean @default(false)
  canUseAdminUI     Boolean @default(false)
  canReadChapters   Boolean @default(false)
  canReadImages     Boolean @default(false)
  users             User[]  @relation("User_role")
}

model Product {
  id                     String  @id @default(uuid()) @postgresql.Uuid
  productTitle           String  @unique @default("")
  description            String  @default("")
  productImage_filesize  Int?
  productImage_extension String?
  productImage_width     Int?
  productImage_height    Int?
  productImage_id        String?
  price                  Int
  priceUnit              String  @default("kr")
  discountPrice          Int?
  recommendedProduct     String  @default("no")
  status                 String  @default("draft")
}

model Project {
  id                     String   @id @default(uuid()) @postgresql.Uuid
  projectTitle           String   @default("")
  shortDescription       String   @default("")
  fullDescription        String   @default("")
  projectImage_filesize  Int?
  projectImage_extension String?
  projectImage_width     Int?
  projectImage_height    Int?
  projectImage_id        String?
  date                   DateTime @postgresql.Date
  location               String   @default("")
  icon                   Json?
}

model Review {
  id         String   @id @default(uuid()) @postgresql.Uuid
  reviewBy   String   @default("")
  reviewText String   @default("")
  date       DateTime @postgresql.Date
  location   String   @default("")
}

model SiteConfig {
  id                  Int     @id
  siteTitle           String  @default("")
  heroPreamble        Json    @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  heroImage_filesize  Int?
  heroImage_extension String?
  heroImage_width     Int?
  heroImage_height    Int?
  heroImage_id        String?
  projectsPreabmle    Json    @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  productsPreabmle    Json    @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  reviewsPreabmle     Json    @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  ourServicesPreabmle Json    @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
  offersPreabmle      Json    @default("[{\"type\":\"paragraph\",\"children\":[{\"text\":\"\"}]}]")
}
